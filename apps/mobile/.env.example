# Copy to .env (git-ignored). Expo reads EXPO_PUBLIC_* vars into the bundle
# at build time — same anon-key-is-safe reasoning as apps/web/.env.example;
# RLS is what actually protects data.
EXPO_PUBLIC_SUPABASE_URL=
EXPO_PUBLIC_SUPABASE_ANON_KEY=
